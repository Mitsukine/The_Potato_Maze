if (place_meeting(x,y,obj_player)) && (chestTriggered = false) {
	if (keyboard_check(vk_space)){
		chestTriggered = true;
		if (loot = obj_enemyChest2) && chestOpened = false {
			instance_create_depth(335, 690, 0, obj_enemyChest2) chestOpened = true;
			}
			
			alarm [1] = 60;
			alarm [2] = 30;
			image_index = 1;
			depth = -y
		}
	}